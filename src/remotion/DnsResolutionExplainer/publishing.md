# DNS 解析：一次查询怎样找到正确服务器

浏览器输入域名后，答案并不会凭空出现：它先检查缓存，再由递归解析器沿着根、顶级域和权威服务器逐级问路，最后把带 TTL 的 IP 记录返回并缓存。

本片是 Agent Producer Roadmap 的最终验收视频，视觉全部由 Remotion 代码和仓库内 manifest-backed SVG 构成；旁白使用 direct VoxCPM，本地音频、review frames、封面和最终视频不进入 Git。
