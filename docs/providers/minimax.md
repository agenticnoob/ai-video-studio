# MiniMax Provider Notes

Status: deprecated.

MiniMax is no longer used by the active staged generation or TTS pipeline.
Current provider notes live in:

- [`docs/providers/deepseek.md`](deepseek.md) for storyboard planning,
  selected-segment replanning, and selected-template compilation.
- [`docs/providers/f5-tts.md`](f5-tts.md) for narration audio and captions.

Do not add new MiniMax environment variables, fallback paths, smoke tests, or
runtime code for the current product stage.
