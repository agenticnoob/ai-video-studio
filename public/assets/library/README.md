# Producer Existing Asset Library

This directory contains reusable, reviewed existing assets only. Every media
file must be referenced by a maintained sample's `ProducerAssetManifest` with
source, license, attribution, SHA-256, size, and media metadata.

Do not place generated images or videos here. Composition-local captures and
working media belong under ignored `public/generated/<slug>/assets/`.
